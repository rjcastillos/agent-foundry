---
applyTo: "**/*.md"
---

# Documentation Instructions

- Use clear headings and concise sections.
- Use repository-relative paths in examples and links.
- Use fenced `text`, `bash`, `python`, or other appropriate code blocks for directory trees and commands.
- Keep directory trees synchronized with the actual repository structure.
- Prefer established terminology: the Foundry uses root-level directories as its canonical source layout; generated projects use applicable `.github/` locations.
- When documenting a convention, explain its scope and whether it applies to this source repository, generated projects, or both.
- Preserve existing front matter when present and avoid duplicate metadata blocks.
- Update related documentation when a path, filename, or workflow changes.
