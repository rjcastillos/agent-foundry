# Workflows

This directory contains reusable workflows for repeatable engineering and
operational processes.

Each workflow should document its purpose, inputs, ordered steps, failure
handling, outputs, and validation procedure. Use
`templates/workflow-template/` when creating a new workflow.
