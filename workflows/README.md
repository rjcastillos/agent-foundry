# Workflows

This directory contains repeatable processes that coordinate agents, skills, prompts, tools, or human review.

Each workflow should document its trigger, inputs, ordered steps, outputs, failure handling, required permissions, and an example invocation.
