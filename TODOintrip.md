feat(sync): filter source files and report synchronization results

Honor source .gitignore rules, skip untracked files, report per-file
actions, and summarize synchronization totals. Update synchronizer
documentation and decision history.