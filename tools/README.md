# Tools

This directory contains utilities for discovering, validating, exporting, or installing catalog assets.

Tools should be safe to run repeatedly, document their inputs and outputs, and avoid modifying canonical assets unless explicitly requested. An installer may copy an asset into a consumer-specific path such as `.agents/`.
